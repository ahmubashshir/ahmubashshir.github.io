{
  "scripts": {
    "badge": "node helper/badges.js",
    "build": "npm run badge",
    "svg": "npm exec svgo static/assets/svg",
    "start": "npm run build && hugo serve --environment production"
  },
  "dependencies": {
    "badge-maker": "^3.3.1",
    "simple-icons": "^5.16.0",
    "svgo": "^2.7.0"
  }
}
