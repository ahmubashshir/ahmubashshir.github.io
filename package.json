{
  "scripts": {
    "badge": "node helper/badges.js",
    "build": "npm run badge",
    "svg": "npm exec svgo static/assets/svg",
    "serve": "hugo serve --environment production",
    "hugo": "mkdir -p public;hugo --minify --environment production --logFile public/build.log",
    "test": "npm run build && npm run serve",
    "start": "npm run build && npm run hugo"
  },
  "dependencies": {
    "badge-maker": "^3.3.1",
    "simple-icons": "^5.16.0",
    "svgo": "^2.7.0"
  }
}
